name: fenv

channels:
  - pyg
  - pytorch
  - nvidia

dependencies:
  - python=3.11
  - pip

  # ML
  - pytorch::pytorch
  - pytorch::torchvision
  - pytorch::torchaudio
  - pyg::pyg
  - pyg::pytorch-sparse
  - pyg::pytorch-cluster
  - conda-forge::lightning
  - conda-forge::torchmetrics
  - wandb

  # Math
  - numpy=1.26.4
  - scipy

  #Analysis
  - pandas

  # Config
  - colorlog
  - hydra-core
  - pyyaml

  # Testing
  - pre-commit
  - pytest
  - pytest-xdist
  - typeguard

  # Other
  - tqdm
